{
    "version": 2,
    "builds": [
        {
        "use": "@vercel/python",
        "config": {
            "maxLambdaSize": "450mb"
        }
        }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/main.py" }
    ]
  }